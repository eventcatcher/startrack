4.1.3 Scrolljump



Durch Doppelklick auf den Scrollslider im Editor,”ffnet sich der Scrolljumpü Dialog. Hier kann man direkt die neue
Scrollposition eingeben,die daran

anschlieáend im SamplewindowüBereich des Editors angezeigt wird.

.

1) max. Time

Anzeige der maximal m”glichen Zeitposition.Diese wird durch die L„nge des
Samples (HDüMode) bzw. Speichers (MemoryüMode) bestimmt.


2) Zeiteingabe

Hier stellt man die Startposition des Samplewindow ein.Die Zeitangabe ist in Stunden, Minuten,Sekunden und Frames
unterteilt,wobei jede Position direkt mit einem Pfeilbutton ver„ndert werden kann.Dabei sind die Doppelpfeil- Buttons
fr die Zehnerpositionen und die Einpfeil-Buttons fr die Einerpositionen zust„ndig.Man kann im Edit-Feld aber auch
direkte Eingaben machen. Fr kleinere Korrekturen sind die Pfeilbuttons aber recht praktisch.


3) Min



Hiermit kann man die SamplewindowüStartposition auf den MinimalüZeitwert setzen (normalerweise Null).

4) Max



Hiermit kann man die SamplewindowüStartposition auf den MaximalüZeitwert (1) bzw. Darstellung des letzten Blocks setzen.



5) aktueller Editslot



Die Dialogbox kann komplett per Tastatur bedient werden.Hier wird das

aktuelle Feld ausgew„hlt (mit CTRL Pfeilülinks bzw. rechts),auf daá sich die Bedienung der Pfeilbuttons per Shortcut
(CTRL Pfeilühoch bzw. runter) bezieht.Die ShortcutüBelegung ist im Anhang unter "Shortcuts" nachzulesen.



6) Help

Das Hilfesystem bzw. der Hypertext wird aufgerufen.Wenn sie nicht wissen wie das funktioniert,schauen sie sich die
Anleitung zu 1stGuide einmal genauer an.


7) Exit

Hiermit wird der Scrolljump-Dialog geschlossen.Die Zeiteingabe wird nicht beachtet und ist ungltig.


8) OK

Hiermit wird der Scrolljump-Dialog geschlossen.Die Zeiteingabe ist
gltig und das Samplewindow wird auf die neue Zeitposition gesetzt.
