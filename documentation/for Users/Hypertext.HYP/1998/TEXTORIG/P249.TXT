4.15.3 TimeüOffset



Dieser Dialog ist fr die Eingabe des Part-Offset zust„ndig.Dieser ”ffnet sich,

sobald man auf den SmpteüEintrag eines Parts klickt.



1) max. Time

Anzeige des maximal m”glichen Offsets (max 24h).



2) Zeiteingabe

Hier stellt man die OffsetüZeit ein.Die Zeitangabe ist in Stunden,Minuten,Sekunden und Frames unterteilt,wobei jede
Position direkt mit einem Pfeilbutton ver„ndert werden kann.Dabei sind die Doppelpfeil-Buttons fr die
Zehnerpositionen,und die Einpfeil-Buttons fr die Einerpositionen zust„ndig.Man kann im Edit-Feld aber auch direkte
Eingaben machen.Fr kleinere Korrekturen sind die Pfeilbuttons aber recht praktisch.


3) Min



Hiermit kann man die Offset auf den MinimalüZeitwert setzen

(normalerweise Null).



4) Max



Hiermit kann man den Offset auf den MaximalüZeitwert (1) setzen.



5) aktueller Editslot



Die Dialogbox kann komplett per Tastatur bedient werden.Hier wird das

aktuelle Feld ausgew„hlt (mit CTRL Pfeilülinks bzw. rechts),auf das sich die Bedienung der Pfeilbuttons per Shortcut
(CTRL Pfeilühoch bzw. runter) bezieht. 

Die ShortcutüBelegung ist im Anhang unter "Shortcuts" nachzulesen.



6) Help

Das Hilfesystem bzw. der Hypertext wird aufgerufen.Wenn sie nicht wissen wie das funktioniert,schauen sie sich die
Anleitung zu 1stGuide einmal genauer an.


7) Exit

Hiermit wird der Offset-Dialog geschlossen.Die Zeiteingabe wird nicht beachtet, und ist ungltig.


8) OK

Hiermit wird der Offset-Dialog geschlossen.Die Offseteingabe ist gltig,und der Part wird um die neue Offset-Zeit
verz”gert.
